# AI Knowledge Assistant Frontend

This Angular frontend connects to the AI Knowledge Assistant backend.

## Commands

- `npm install` to install dependencies
- `npm start` to run locally on `http://localhost:4200`
- `npm run build` to build the app
